---
description: |
    A step-by-step procedure executed exactly as written, in order, when invoked
    — never skipped, reordered, or improvised. Use for repeatable routines:
    conflict checks, defined-term sweeps, filing routines. Give it an id and
    invoke it by name.
    Example:
        # action defined-term-sweep {#dt_sweep}
        1. List every capitalized term
        2. Confirm each has exactly one definition
        3. Flag any used-but-undefined or defined-but-unused term
synonyms:
    - actions
    - procedure
    - procedures
---

<reusable_procedure name="{name}" id="{id}">

A step-by-step procedure. When instructed to perform it, execute the steps exactly as written, in order — never skip, reorder, or improvise steps:

{body}

{children}

</reusable_procedure>
